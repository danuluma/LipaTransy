<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user.lipatransyapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/lipa_launcher_foreground"
        android:label=""
        android:roundIcon="@mipmap/lipa_launcher_foreground"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".Splash_Screen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Intro_Activity" />
        <activity android:name=".ConductorActivity"></activity>



    </application>

</manifest>